var canvas = document.getElementById('the-canvas');
var ctx = canvas.getContext('2d');

ctx.fillStyle = "white";

ctx.rotate(25/180*Math.PI);
ctx.scale(0.5, 1);

ctx.beginPath();
ctx.arc((1 / 0.5) * 100, 100, 75, 0, 2 * Math.PI);

ctx.fill();
ctx.stroke();